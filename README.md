# Example

To run this example:

- `npm install` or `yarn`
- `npm start` or `yarn start`


Settings
- Actions -> General
-   Workflow permissions
-     Read and write permissions

- Pages
-   Branch
-     gh-pages
